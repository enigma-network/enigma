FROM golang:1.23-alpine AS builder
WORKDIR /app
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN CGO_ENABLED=0 GOOS=linux go build -ldflags="-s -w" -o enigma-server ./cmd/server

FROM alpine:3.19
RUN apk add --no-cache ca-certificates
COPY --from=builder /app/enigma-server /usr/local/bin/enigma-server
EXPOSE 8080
VOLUME ["/data"]
ENTRYPOINT ["enigma-server", "-db", "/data/enigma.db", "-log", "/data/enigma.log"]
